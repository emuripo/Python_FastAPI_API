# Code Challenge Header

# Build a simple API using Python and FastAPI to manage documents.
- The API should allow uploading a document, processing it into chunks, checking its status, and retrieving the generated chunks.